#include <stdio.h>

int main()
{ /*
     int n1, n2, max = 0;
     printf("Enter number -");
     scanf("%d %d", &n1, &n2);
     if (n1 > n2)
     {
         max = n1;
     }
     else
     {
         max = n2;
     }

     while (1)
     {
         if (max % n1 == 0 && max % n2 == 0)
         {
             printf("LCM is a - %d", max);
             break;
         }
         ++max;
     }
 */
    return 0;
}
