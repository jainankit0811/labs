#include <stdio.h>

int main() /*
 {
     int n1, n2, n;
     printf("Enter nuber -");
     scanf("%d %d", &n1, &n2);

     for (int i = 2; i <= n1 || i <= n2; i++)
     {
         = n1 / i;
         if (n1 == i)
         {
             printf("%d%d\n", n1, n2);
         }
     }
     return 0;
 }*/
{
    int n;
    printf("Enter number -");
    scanf("%d", &n);
    for (int i = ; i <= n; i)
    {
        n / i == 1;
        printf("%d\n", i);
    }
    return 0;
}
