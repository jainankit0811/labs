
#include <stdio.h>

int main()
{
    int n, i;
    printf("Enter number.\n");
    scanf("%d", &n);
    for (int i = 0; i <= n; i++)
    {
        for (int j = ; j <= i; j++)
        {
            if (j % i == 0)
            {
                /* code */
            }
        }
    }

    return 0;
}